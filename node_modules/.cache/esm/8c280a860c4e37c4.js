let database,Animal;_0c4‍.w("./public/JavaScripts/animaldb.js",[["*",null,function(v){database=v}]]);_0c4‍.w("./public/JavaScripts/animal.js",[["Animal",["Animal"],function(v){Animal=v}]]);


// Load the data from the JSON file
let data = require('./public/JavaScripts/products.json');

var mysql = require('mysql');
const db = require('./schema.js');

// Open a connection to the database
var con = mysql.createConnection({
    host: "localhost",
    user: "root",
    password: "Student1!",
    database: "products"
});


// Ensure that tables are created and initialized
db.initDB(con);
function importProductData(con)
{
    for (var k in data['animal']) {
        database.insertAnimal(con, k, data['animal'][k]);
    }
}

importProductData(con);