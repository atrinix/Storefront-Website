let database;_f96‍.w("./animaldb.js",[["*",null,function(v){database=v}]]);

var mysql = require('mysql');

// Open a connection to the database
var con = mysql.createConnection({
    host: "localhost",
    user: "root",
    password: "Student1!",
    database: "products"
});

function printResultNames(result) {
    if (result) {
        for (var r of result) {
            _f96‍.g.console.log(r.name);
        }
    } else {
        console.log("No result");
    }
}

//database.readAnimalByKey(con, "platypus", cb);
//database.readAnimalsByKingdom(con, "mammal", printResultNames);
let terms = ["cold", "blooded", "tiger"];
for (var t of terms) {
    database.readAnimalsBySearchTerm(con, t, printResultNames);
}

database.readAllAnimals(con, printResultNames);
